# Keyforge

> Visual keyframe animation editor for CSS and Web Animations API

Keyforge is a browser-based animation editor that lets you craft, preview, and export real CSS keyframe animations and Web Animations API code through an intuitive timeline UI.

## Stack

- **SolidJS** + TypeScript — signal-based reactivity, no VDOM overhead
- **Vite** — instant HMR, fast builds
- **Modern CSS** — custom properties, layers, container queries, no utility framework

## Getting Started

```bash
npm install
npm run dev
```

## Scripts

| Command             | Description              |
| ------------------- | ------------------------ |
| `npm run dev`       | Start dev server         |
| `npm run build`     | Production build         |
| `npm run preview`   | Preview production build |
| `npm run typecheck` | TypeScript check         |
| `npm run lint`      | ESLint                   |
| `npm run format`    | Prettier                 |

## Docs

See [`docs/PLAN.md`](docs/PLAN.md) for product vision and roadmap.
