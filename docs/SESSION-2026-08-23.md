# Session Summary — 2026-08-23

> Autonomous build day. 27 PRs merged (#12–#48 range), tests 41 → 168.
> Two research passes, one UX audit, and continuous verified delivery.

## Feature timeline (merged in order)

### Morning — DevTools Token UI stabilization

| PR  | What                                                                                                                                                                   |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| #14 | DevTools Token UI (Phase 1.5 §4) after full QA pass: storage pin, dead-code removal, lint-to-zero, transform display fix, a11y baseline, docs truth-updates, +32 tests |
| #22 | upload-artifact v4→v7 (redo of closed #3)                                                                                                                              |
| #23 | dependabot: ignore TS/@typescript-eslint majors                                                                                                                        |

### Afternoon — Usefulness wave ("Wave 2.5")

| PR  | What                                                                                                          |
| --- | ------------------------------------------------------------------------------------------------------------- |
| #26 | `--color-text-faint` defined (consumed-but-undeclared bug)                                                    |
| #27 | PLAN.md Phase 1.5 marked complete                                                                             |
| #28 | **localStorage autosave + restore** · **guided empty state + sample animation** · **smart keyframe defaults** |
| #29 | DocBar: document rename, JSON export/import                                                                   |
| #30 | Click-to-edit duration · layer rename affordance                                                              |
| #31 | **Pose-to-pose**: + KF captures the interpolated playhead pose                                                |

### Evening — Research-driven + inspector audit

Research: competitive landscape (20 searches) → CSS import identified as #1 gap; modern CSS techniques (22 searches) → springs/scroll-driven/@property ranked.

| PR  | What                                                                                                                                  |
| --- | ------------------------------------------------------------------------------------------------------------------------------------- |
| #32 | **CSS import**: paste @keyframes → editable document                                                                                  |
| #33 | **Lossless round-trip**: export→import→export byte-identical; %→ms conversion, prefix stripping, fill-stop dedup                      |
| #34 | **Spring easings**: damped-oscillator → linear() generator, perceptual model (visualDuration+bounce), 5 presets, EasingEditor section |
| #35 | Reduced-motion-safe CSS export (CSS·RM button)                                                                                        |
| #36 | dependabot ignores @solid-primitives/storage (protects ~4.3 pin)                                                                      |

### Night — Inspector UX audit execution

Audit (`docs/INSPECTOR-UX-PLAN.md`) found a structural defect (row DOM teardown on every commit) + correctness bugs. All high-severity items fixed:

| PR  | What                                                                                          |
| --- | --------------------------------------------------------------------------------------------- |
| #38 | Named easings resolve to real editor handles                                                  |
| #39 | **Stable row identity** — commits no longer rebuild rows; editors survive drags/preset clicks |
| #40 | Validation guards — empty values/negative times rejected at UI + store layers                 |
| #41 | Track removal (user-reported gap)                                                             |
| #42 | Property metadata registry — unit dropdowns filter per property (no more `opacity: 0vw`)      |
| #43 | **Transform stack composer** — add/remove/reorder functions                                   |
| #44 | Safe scrubbing — drag to change numbers (4px tap threshold, Shift/Alt modifiers, mouse v1)    |
| #45 | Layout fix: .app wrapper restored, shell fills viewport                                       |
| #46 | Co-timed stops with conflicting easings export as per-track rules                             |
| #47 | Keyboard ↑↓ nudging on number chips                                                           |
| #48 | Uniform Escape contract for the easing editor                                                 |

## Numbers

- **PRs merged: 27** · Tests: **41 → 168**
- Every feature verified three ways: unit tests, lint/typecheck/build, live browser interaction via CDP with screenshot evidence.

## Open items (from audit plan, deliberately paused)

- Multi-part values framework (box-shadow/filter tracks) — L
- Undo/redo — M/L; **recommended to scope with Nicolas first** (snapshot-based via single write path vs granular inverse ops)
- Tab/`[`/`]` token chaining; keyboard map doc
- From modern-CSS research: scroll-driven timeline mode, @property tracks (next feature bets)

## Known limitations

- Pose interpolation approximates linear() springs as linear (documented)
- Scrubbing is mouse-only v1 (touch keeps tap-to-edit)
- Firefox lacks scroll-driven animations until ~late 2026 (export emits @supports guards when that feature lands)
